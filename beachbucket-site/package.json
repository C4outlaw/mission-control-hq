{
  "name": "beachbucket-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate:assets": "node scripts/validate-assets.mjs",
    "build": "npm run validate:assets && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
