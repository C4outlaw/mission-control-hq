import MarvinRoomClient from './MarvinRoomClient';

export const metadata = {
  title: 'Marvin Room | Myrie HQ',
  description: 'Agent operations, voice command capture, and local automation room.',
};

export default function MarvinRoomPage() {
  return <MarvinRoomClient />;
}
